package de.hhu.stups.datagenerator

import org.slf4j.LoggerFactory

class OperationDataGenerator {

    private val logger = LoggerFactory.getLogger(javaClass)
    //private val injector = Guice.createInjector(Stage.PRODUCTION, de.hhu.stups.DataGeneratorModule, MainModule())

    fun generateData() {
        //val api = injector.getInstance(Api::class.java)

    }
}